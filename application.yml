spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
    application:
      name: micro-service-cloud-config-dev
---
spring:
  profiles: test
    application:
      name: microservicecloud-config-test
